---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Contato — Conselho Shiba Inu Brasil">

  <section class="section-kamon">
    <h1>Contato</h1>

    <dl class="contact-list">
      <div class="contact-row">
        <dt>E-mail</dt>
        <dd>Em breve</dd>
      </div>
      <div class="contact-row">
        <dt>Instagram</dt>
        <dd>
          <a href="https://www.instagram.com/cbr.shibainu" class="instagram-link" target="_blank" rel="noopener noreferrer" aria-label="Instagram do Conselho">
            <svg viewBox="0 0 24 24" class="icon-instagram" aria-hidden="true" focusable="false">
              <path d="M7 2h10a5 5 0 015 5v10a5 5 0 01-5 5H7a5 5 0 01-5-5V7a5 5 0 015-5z" fill="none" stroke="currentColor" stroke-width="1.5"/>
              <circle cx="12" cy="12" r="3.5" fill="none" stroke="currentColor" stroke-width="1.5"/>
              <circle cx="17.5" cy="6.5" r="1" fill="currentColor"/>
            </svg>
            <span>@cbr.shibainu</span>
          </a>
        </dd>
      </div>
    </dl>

    <div class="line-divider"></div>

    <p class="notice">
      Estamos estruturando os canais oficiais do Conselho.
      Em breve disponibilizaremos e-mail e demais canais de contato.
    </p>
  </section>

</BaseLayout>

<style>
  .contact-list {
    margin: 1.5rem 0 0;
    padding: 0;
  }
  .contact-row {
    display: grid;
    grid-template-columns: 120px 1fr;
    gap: 12px;
    padding: 14px 0;
    border-bottom: 1px solid var(--border);
    align-items: center;
  }
  .contact-row dt {
    margin: 0;
    font-weight: 600;
    color: var(--ink-blue);
    font-size: 0.9rem;
    letter-spacing: 0.2px;
  }
  .contact-row dd {
    margin: 0;
    color: var(--text-muted);
  }
  .instagram-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: var(--text-main);
    text-decoration: none;
    transition: color 0.25s ease;
  }
  .instagram-link:hover {
    color: var(--accent);
  }
  .icon-instagram {
    width: 20px;
    height: 20px;
  }
  .notice {
    color: var(--text-muted);
    font-style: italic;
    font-size: 0.95rem;
  }
</style>
