import { defineConfig } from 'astro/config';

export default defineConfig({
  outDir: 'dist',
  // Atualize para o domínio final quando ativo (ex.: 'https://shibainu.org.br/')
  site: 'https://site-conselho-brasil.github.io/',
});

